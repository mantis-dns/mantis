# Mantis -- Tasks

> Ordered work breakdown derived from IMPLEMENTATION.md.
> Execute sequentially. Each task is completable in a single session.

## Summary

| Metric               | Value                 |
|----------------------|-----------------------|
| Total Tasks          | 48                    |
| Phases               | 10                    |
| Foundation Complete  | After Task 4          |
| DNS Core Complete    | After Task 9          |
| Backend MVP Complete | After Task 27         |
| Frontend MVP         | After Task 39         |
| Full Release         | After Task 48         |

---

## Phase 1: Project Foundation

> Establishes project structure, core types, configuration, and storage.
> After this phase: project compiles, Pebble opens, config loads. No functionality yet.

### Task 1: Go Project Scaffolding

**Create the Go module, directory structure, and all configuration files.**

**Files to create:**
- `go.mod` -- Module `github.com/[org]/mantis`, Go 1.26, all deps from IMPLEMENTATION.md SS1.3
- `go.sum` -- Generated by `go mod tidy`
- `.gitignore` -- Go build artifacts, data/, dist/, web/node_modules/, web/dist/, .env
- `Makefile` -- Targets: build, test, lint, dev, clean, build-frontend, build-all
- `LICENSE` -- MIT or Apache-2.0 (user choice)
- `README.md` -- Minimal: project name, one-liner description, build instructions
- `configs/mantis.example.toml` -- Example config with all settings and comments
- `cmd/mantis/main.go` -- Minimal main() that prints version and exits
- All directories from IMPLEMENTATION.md SS3.1 (empty packages with doc.go)

**Commands to run:**
```bash
go mod init github.com/[org]/mantis
go mod tidy
go build ./cmd/mantis
```

**Acceptance Criteria:**
- [ ] `go build ./cmd/mantis` compiles without errors
- [ ] `go vet ./...` passes
- [ ] All directories from IMPLEMENTATION.md SS3.1 exist
- [ ] `./mantis --version` prints version string
- [ ] `.gitignore` covers data/, dist/, web/node_modules/, .env

**Dependencies:** None
**Effort:** 1 hour
**Refs:** IMPLEMENTATION.md SS1, SS3.1

---

### Task 2: Core Domain Types

**Define all domain entities, interfaces, and error types in internal/domain/.**

**Files to create:**
- `internal/domain/query.go` -- Query, Response, Transport enum, QueryType constants
- `internal/domain/blocklist.go` -- BlocklistSource, CustomRule entities
- `internal/domain/lease.go` -- DhcpLease entity
- `internal/domain/client.go` -- Client entity (network device with hostname, MAC, query count)
- `internal/domain/stats.go` -- StatsSummary, StatsOverTime, TopDomain, TopClient types
- `internal/domain/config.go` -- Settings entity (key/value)
- `internal/domain/session.go` -- Session, APIKey entities
- `internal/domain/repository.go` -- All repository interfaces: QueryLogRepository, BlocklistRepository, CustomRuleRepository, LeaseRepository, SettingsRepository, SessionRepository
- `internal/domain/errors.go` -- ErrBlocked, ErrNotFound, ErrUpstreamTimeout, ErrPoolExhausted, ErrUnauthorized, ErrRateLimited, ErrValidation, ErrInternal

**Code requirements:**
- Every entity from SPECIFICATION.md SS5.1 has a Go struct with JSON tags
- Repository interfaces cover all CRUD + custom query operations
- Domain errors implement `error` interface with code field for API mapping
- No external dependencies -- only stdlib imports (net, time, errors, fmt)

**Acceptance Criteria:**
- [ ] `go build ./internal/domain/...` compiles with no errors
- [ ] All 5 entity types from SPECIFICATION.md SS5.1 have corresponding structs
- [ ] All 6 repository interfaces defined with complete method signatures
- [ ] Error types implement `error` and support `errors.Is`/`errors.As`
- [ ] `go vet ./internal/domain/...` passes

**Dependencies:** Task 1
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS2, SS5; IMPLEMENTATION.md SS2.1, SS7

---

### Task 3: Configuration System

**Implement configuration loading from TOML file, environment variables, and CLI flags.**

**Files to create:**
- `internal/config/config.go` -- Config struct with all fields from IMPLEMENTATION.md SS8.2, sensible defaults
- `internal/config/loader.go` -- Load() function: parse TOML → overlay env vars (MANTIS_ prefix) → overlay CLI flags
- `internal/config/validate.go` -- Validate() function: port ranges, IP format, non-empty required fields
- `internal/config/config_test.go` -- Unit tests for loading priority and validation

**Code requirements:**
- Use pelletier/go-toml/v2 for TOML parsing
- Environment variables follow MANTIS_ prefix convention with underscore mapping (e.g., MANTIS_DNS_LISTEN_ADDRESS)
- Use stdlib `flag` package for CLI flags
- Merge priority: flags > env > file > defaults (IMPLEMENTATION.md SS8.1)
- Config struct uses strong types (net.IP, time.Duration, etc.)

**Acceptance Criteria:**
- [ ] Config loads from TOML file with all fields from IMPLEMENTATION.md SS8.2
- [ ] Environment variables override TOML values
- [ ] CLI flags override both
- [ ] Missing config file uses defaults without error
- [ ] Invalid config (bad IP, negative port) returns descriptive error
- [ ] `go test ./internal/config/...` passes

**Dependencies:** Task 1
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.6; IMPLEMENTATION.md SS8

---

### Task 4: Pebble Storage Layer

**Set up Pebble database lifecycle and implement all repository interfaces.**

**Files to create:**
- `internal/storage/pebble.go` -- NewDB(), Close(), key prefix constants, Pebble options tuning
- `internal/storage/keys.go` -- Key encoding/decoding helpers (time-ordered keys, prefix formatting)
- `internal/storage/querylog.go` -- PebbleQueryLogRepository with batch writer
- `internal/storage/blocklist.go` -- PebbleBlocklistRepository
- `internal/storage/rules.go` -- PebbleCustomRuleRepository
- `internal/storage/leases.go` -- PebbleLeaseRepository with MAC and IP indexes
- `internal/storage/settings.go` -- PebbleSettingsRepository
- `internal/storage/sessions.go` -- PebbleSessionRepository (session + API key storage)
- `internal/storage/querylog_test.go` -- Tests for batch write and time-range query
- `internal/storage/leases_test.go` -- Tests for CRUD and cross-index lookup

**Code requirements:**
- Key format from IMPLEMENTATION.md SS4.1 (prefix:sort_key pattern)
- Query log uses batch writer from IMPLEMENTATION.md SS4.3 (flush every 1000 entries or 100ms)
- Each repository method maps domain.Repository interface
- JSON serialization for low-volume entities, consider protobuf for query log later
- All repos accept `*pebble.DB` in constructor

**Acceptance Criteria:**
- [ ] Pebble opens and closes cleanly with temp directory
- [ ] QueryLogRepository: Append 10,000 entries, Query by time range returns correct subset
- [ ] LeaseRepository: Create lease, lookup by MAC, lookup by IP (cross-index)
- [ ] SettingsRepository: Get/Set round-trip works for all value types
- [ ] `go test ./internal/storage/...` passes with all tests
- [ ] No data corruption on concurrent writes from multiple goroutines

**Dependencies:** Task 2
**Effort:** 5 hours
**Refs:** SPECIFICATION.md SS5; IMPLEMENTATION.md SS4, SS2.6

---

## Phase 2: DNS Core

> Implements the fundamental DNS server, cache, forwarding, and blocking pipeline.
> After this phase: Mantis resolves DNS queries with blocking and caching.

### Task 5: Basic DNS Server (UDP/TCP)

**Implement a DNS server that listens on UDP/TCP and responds to queries using miekg/dns.**

**Files to create:**
- `internal/dns/server.go` -- DNS server with Start()/Stop(), listens on configurable address
- `internal/dns/handler.go` -- ServeDNS implementation: converts dns.Msg to domain.Query, calls resolver
- `internal/dns/writer.go` -- Converts domain.Response back to dns.Msg
- `internal/dns/server_test.go` -- Integration test: start server, send query, verify response

**Code requirements:**
- Use miekg/dns (codeberg.org/miekg/dns) Server for UDP and TCP
- Handler converts between dns.Msg and domain types (IMPLEMENTATION.md SS2.1)
- Accept a domain.Resolver interface in constructor (for now, use a stub that returns SERVFAIL)
- Graceful shutdown: drain in-flight queries on Stop()
- Support EDNS0 with 4096 byte buffer

**Acceptance Criteria:**
- [ ] DNS server listens on configurable UDP and TCP port
- [ ] Responds to A, AAAA, MX, TXT query types
- [ ] Query from `dig @localhost -p [port] example.com` gets a response (SERVFAIL for now)
- [ ] Graceful shutdown completes within 5 seconds
- [ ] Concurrent queries from multiple goroutines don't panic
- [ ] `go test ./internal/dns/...` passes

**Dependencies:** Task 2
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.1.1; IMPLEMENTATION.md SS2.1, SS3.2 (dns module)

---

### Task 6: DNS Response Cache

**Implement a TTL-aware DNS response cache with LRU eviction.**

**Files to create:**
- `internal/resolver/cache.go` -- DNSCache struct: Get(), Set(), thread-safe, TTL-aware eviction, configurable max entries
- `internal/resolver/cache_test.go` -- Tests for TTL expiry, LRU eviction, concurrent access

**Code requirements:**
- Use sync.RWMutex for thread safety (read-heavy workload)
- Cache key: lowercase(domain) + queryType
- Cache value: dns response + insertion time + original TTL
- Eviction: expired entries removed on access + periodic sweep (every 60s). LRU when at capacity.
- Configurable max entries (default 10,000 from SPEC)

**Acceptance Criteria:**
- [ ] Cache hit returns stored response within TTL
- [ ] Cache miss returns nil
- [ ] Expired entries return nil (cache miss)
- [ ] Cache at max capacity evicts least recently used entry
- [ ] Concurrent Get/Set from 100 goroutines: no races (run with -race)
- [ ] `go test -race ./internal/resolver/...` passes

**Dependencies:** Task 2
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS3.1.1 (cache); IMPLEMENTATION.md SS4.4

---

### Task 7: Upstream DNS Forwarding

**Implement DNS query forwarding to configurable upstream servers with failover and circuit breaker.**

**Files to create:**
- `internal/resolver/forwarder.go` -- Forwarder struct: implements domain.Resolver, parallel query to upstreams, uses fastest response
- `internal/resolver/circuit.go` -- CircuitBreaker: Closed/Open/HalfOpen states, failure threshold, cooldown
- `internal/resolver/forwarder_test.go` -- Tests for parallel resolution, failover, circuit breaker state transitions
- `internal/resolver/circuit_test.go` -- Unit tests for circuit breaker logic

**Code requirements:**
- Parallel query to all configured upstreams (SPEC SS3.1.4: "use fastest response")
- Circuit breaker per upstream (IMPLEMENTATION.md SS2.5): open after 5 consecutive failures, cooldown 30s
- Support plain DNS (UDP/TCP), DoH, and DoT upstream protocols
- Timeout per upstream query: configurable (default 5s)
- Health check goroutine: ping each upstream every 30s with a simple query

**Acceptance Criteria:**
- [ ] Forwards query to upstream and returns response
- [ ] Parallel query: uses fastest upstream response, cancels others
- [ ] Circuit breaker opens after 5 failures, rejects immediately for 30s
- [ ] Circuit breaker half-open: allows one test query after cooldown
- [ ] All upstreams down: returns SERVFAIL
- [ ] Upstream timeout: fails over to next available upstream within configured timeout
- [ ] `go test ./internal/resolver/...` passes

**Dependencies:** Task 5, Task 6
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS3.1.4; IMPLEMENTATION.md SS2.3, SS2.5

---

### Task 8: Gravity Engine (Blocklist Lookup)

**Implement the Gravity engine: radix tree for domain lookup with wildcard support.**

**Files to create:**
- `internal/gravity/tree.go` -- DomainTree: radix tree wrapping hashicorp/go-immutable-radix. Reversed domain keys for suffix matching. Lookup(), Contains(), WildcardMatch()
- `internal/gravity/engine.go` -- GravityEngine: holds current tree (atomic pointer swap), Lookup(), Rebuild() from domain lists, AddAllowRule(), AddBlockRule()
- `internal/gravity/engine_test.go` -- Tests for exact match, wildcard match, allowlist override

**Code requirements:**
- Domains stored reversed ("com.example.ads") for efficient suffix/wildcard lookup (IMPLEMENTATION.md SS1.2)
- Use hashicorp/go-immutable-radix for lock-free concurrent reads during rebuild
- Atomic tree swap: new tree built in background, pointer swapped atomically (readers unblocked)
- Allowlist checked before blocklist on every lookup (SPEC SS3.2.2)
- Support exact domain match, wildcard (*.ads.example.com), and regex patterns

**Acceptance Criteria:**
- [ ] Exact domain lookup in <1us for tree with 1M entries
- [ ] Wildcard `*.ads.example.com` matches `sub.ads.example.com` and `deep.sub.ads.example.com`
- [ ] Allowlisted domain returns false even if on blocklist
- [ ] Rebuild from 1M domains completes in <5s
- [ ] Concurrent reads during rebuild don't block or return stale results
- [ ] `go test -race ./internal/gravity/...` passes with -race
- [ ] Memory for 2M domains is <256MB

**Dependencies:** Task 2
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS3.2; IMPLEMENTATION.md SS1.2, SS2.8 (gravity module)

---

### Task 9: DNS Query Pipeline (Chain of Responsibility)

**Wire up the DNS query processing chain: cache check -> gravity check -> upstream resolution.**

**Files to create:**
- `internal/pipeline/handler.go` -- QueryHandler interface with Handle(ctx, query, next) signature
- `internal/pipeline/chain.go` -- Chain struct: ordered list of handlers, implements domain.Resolver
- `internal/pipeline/cache.go` -- CacheHandler: check cache first, store response after resolution
- `internal/pipeline/gravity.go` -- GravityHandler: check Gravity blocklist, return 0.0.0.0 if blocked
- `internal/pipeline/clientrule.go` -- ClientRuleHandler: per-client blocking toggle
- `internal/pipeline/upstream.go` -- UpstreamHandler: delegate to Forwarder or Recursive resolver
- `internal/pipeline/chain_test.go` -- Integration test: blocked domain returns 0.0.0.0, allowed domain forwards

**Code requirements:**
- Chain of Responsibility from IMPLEMENTATION.md SS2.2
- Pipeline order: CacheHandler -> GravityHandler -> ClientRuleHandler -> UpstreamHandler
- CacheHandler: on cache hit, return immediately. On miss, pass to next, then cache the result.
- GravityHandler: lookup domain in Gravity. If blocked, return 0.0.0.0 (A) or :: (AAAA).
- Each handler wraps the next handler, calling next.Handle() if it can't resolve.

**Wire main.go:**
- Update cmd/mantis/main.go: load config -> open Pebble -> build pipeline -> start DNS server -> block on signal

**Acceptance Criteria:**
- [ ] Blocked domain returns 0.0.0.0 for A query and :: for AAAA query
- [ ] Non-blocked domain forwards to upstream and returns correct response
- [ ] Second query for same domain hits cache
- [ ] Pipeline handles 10,000 concurrent queries without panic or race
- [ ] `dig @localhost -p [port] [blocked-domain]` returns 0.0.0.0
- [ ] `dig @localhost -p [port] google.com` returns real IP
- [ ] `go test ./internal/pipeline/...` passes

**Dependencies:** Task 5, Task 6, Task 7, Task 8
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS3.1; IMPLEMENTATION.md SS2.2, SS3.2 (pipeline module)

---

## Phase 3: Blocklist Management

> Downloads, parses, and compiles blocklists. Query logging via event bus.
> After this phase: Mantis blocks ads from real blocklists, logs queries.

### Task 10: Blocklist Downloader and Parser

**Implement HTTP blocklist download and multi-format parsing.**

**Files to create:**
- `internal/gravity/downloader.go` -- Download() function: HTTP GET with timeout, retry 3x with backoff
- `internal/gravity/parser.go` -- Parse(): supports hosts-file, domain-only, and adblock formats. Returns []string domains.
- `internal/gravity/parser_test.go` -- Tests for each format, comment lines, malformed lines
- `internal/gravity/downloader_test.go` -- Tests with httptest server for success, 404, timeout

**Code requirements:**
- Worker pool pattern (IMPLEMENTATION.md SS2.8): max 5 concurrent downloads
- Hosts-file format: extract domain from `0.0.0.0 domain.com` or `127.0.0.1 domain.com` lines
- Domain-only: one domain per line, skip comments (#) and empty lines
- Adblock: extract domain from `||domain.com^` patterns
- Skip invalid lines, count parse errors per source

**Acceptance Criteria:**
- [ ] Downloads blocklist from HTTP URL with 30s timeout
- [ ] Retries 3 times on failure with exponential backoff
- [ ] Parses hosts-file format correctly (extracts ~100K domains from StevenBlack list)
- [ ] Parses domain-only format correctly
- [ ] Parses basic adblock format (||domain^)
- [ ] Comment lines and empty lines are skipped
- [ ] Returns parse error count alongside valid domains
- [ ] `go test ./internal/gravity/...` passes

**Dependencies:** Task 8
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.2.1; IMPLEMENTATION.md SS2.8

---

### Task 11: Gravity Rebuild Orchestrator

**Wire downloader + parser + tree into full Gravity rebuild flow with persistence.**

**Files to modify:**
- `internal/gravity/engine.go` -- Add Rebuild() method: download all sources -> parse -> deduplicate -> build tree -> atomic swap
- `internal/gravity/scheduler.go` -- Periodic rebuild scheduler (configurable interval, default 24h)

**Files to create:**
- `internal/gravity/scheduler.go` -- GravityScheduler: runs Rebuild on timer, supports manual trigger

**Code requirements:**
- Rebuild(): download all enabled BlocklistSources from storage, parse each, deduplicate across all, merge with custom block rules, build new radix tree, atomic swap
- On download failure: keep previous version of that source, log warning
- Persist blocklist metadata (domain count, last update, status) to storage after rebuild
- Ship 5+ default blocklist URLs as constants (StevenBlack unified, EasyList, EasyPrivacy, etc.)
- Scheduler uses time.Ticker, cancellable via context

**Acceptance Criteria:**
- [ ] Rebuild downloads from multiple real blocklist URLs
- [ ] Deduplicates across sources (total < sum of individual counts)
- [ ] Failed download keeps previous domains, rebuilds with others
- [ ] Blocklist metadata persisted to Pebble (domain count, status, timestamp)
- [ ] Scheduler triggers rebuild at configured interval
- [ ] Manual rebuild trigger works via engine.Rebuild()
- [ ] Default blocklist URLs are reasonable and accessible

**Dependencies:** Task 4, Task 8, Task 10
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.2.1; IMPLEMENTATION.md SS3.2 (gravity module)

---

### Task 12: Custom Allow/Block Rules

**Implement user-defined allow and block rules with immediate effect.**

**Files to modify:**
- `internal/gravity/engine.go` -- Add AddRule(), RemoveRule() methods that update the live tree without full rebuild

**Code requirements:**
- Custom rules stored in Pebble via CustomRuleRepository (Task 4)
- Adding a rule: persist to Pebble + update live Gravity tree immediately
- Removing a rule: delete from Pebble + rebuild tree (or remove from tree if supported)
- Allowlist rules checked first in Gravity lookup (SPEC SS3.2.2)
- Support exact domain, wildcard (*.example.com), and regex patterns

**Acceptance Criteria:**
- [ ] Add custom block rule: domain immediately blocked
- [ ] Add custom allow rule: previously blocked domain immediately allowed
- [ ] Remove rule: domain returns to normal blocklist behavior
- [ ] Rules survive Gravity rebuild (persisted in Pebble)
- [ ] Rules survive application restart (loaded from Pebble on startup)
- [ ] `go test ./internal/gravity/...` passes

**Dependencies:** Task 4, Task 8
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS3.2.2, SS3.2.3

---

### Task 13: Event Bus and Query Logging

**Implement the event bus for query events and the query log writer.**

**Files to create:**
- `internal/event/types.go` -- QueryEvent struct (timestamp, client IP, domain, type, result, latency, upstream, answer)
- `internal/event/bus.go` -- Bus struct: Publish(), Subscribe(), Unsubscribe(). Channel-based, non-blocking publish.
- `internal/event/bus_test.go` -- Tests for publish/subscribe, slow subscriber (dropped events), concurrent access

**Files to modify:**
- `internal/pipeline/chain.go` -- After each query resolution, publish QueryEvent to event bus
- `internal/storage/querylog.go` -- Add QueryLogWriter: subscribes to event bus, batch writes to Pebble

**Code requirements:**
- Event bus from IMPLEMENTATION.md SS2.4: channel-based, fan-out to all subscribers
- Non-blocking publish: if subscriber channel is full, drop event (don't block DNS resolution)
- Query log writer: subscribes to bus, batches writes (1000 entries or 100ms flush interval)
- Configurable log level: all, blocked-only, none (from config)
- Privacy mode: mask last octet of client IP when enabled

**Acceptance Criteria:**
- [ ] Query events published for every DNS resolution
- [ ] Multiple subscribers receive same event
- [ ] Slow subscriber doesn't block publisher or other subscribers
- [ ] Query log entries persisted to Pebble with correct key ordering
- [ ] Query log retrievable by time range
- [ ] Privacy mode masks client IP (192.168.1.100 -> 192.168.1.0)
- [ ] `go test -race ./internal/event/...` passes

**Dependencies:** Task 4, Task 9
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.4.1; IMPLEMENTATION.md SS2.4

---

## Phase 4: Advanced DNS

> Adds recursive resolution, encrypted DNS (DoH/DoT), and TLS management.
> After this phase: Mantis supports all DNS transport modes.

### Task 14: Recursive DNS Resolver

**Implement iterative DNS resolution from root hints.**

**Files to create:**
- `internal/resolver/recursive.go` -- Recursive struct: implements domain.Resolver, iterative resolution from root hints
- `internal/resolver/roothints.go` -- Embedded root hints file, parser, NS list
- `internal/resolver/recursive_test.go` -- Integration tests with real DNS resolution

**Code requirements:**
- Start from embedded root hints (IANA root servers)
- Follow NS referrals iteratively: root -> TLD -> authoritative
- Cache intermediate NS records and glue records in DNS cache (Task 6)
- Detect and bail on circular CNAME chains (max depth 30)
- Detect lame delegation: try alternative NS
- Apply Gravity blocking at each resolution step

**Acceptance Criteria:**
- [ ] Resolves `google.com` A record starting from root hints (no upstream configured)
- [ ] Resolves CNAME chains correctly
- [ ] Bails on circular CNAME chain after max depth
- [ ] Caches NS records for subsequent queries (second query faster)
- [ ] Blocked domain returns 0.0.0.0 even in recursive mode
- [ ] Max recursion depth of 30 enforced
- [ ] `go test ./internal/resolver/...` passes

**Dependencies:** Task 6, Task 8
**Effort:** 5 hours
**Refs:** SPECIFICATION.md SS3.1.5; IMPLEMENTATION.md SS2.3, SS3.2 (resolver module)

---

### Task 15: TLS Certificate Management

**Implement TLS certificate loading and Let's Encrypt ACME auto-provisioning.**

**Files to create:**
- `internal/tls/manager.go` -- TLSManager: loads cert from files, provides tls.Config for DoH/DoT
- `internal/tls/acme.go` -- ACME wrapper: uses golang.org/x/crypto/acme/autocert for Let's Encrypt
- `internal/tls/manager_test.go` -- Tests with self-signed certs

**Code requirements:**
- Support manual cert: load from certFile + keyFile paths in config
- Support ACME: autocert.Manager with configurable domain and email
- Shared tls.Config for both DoH and DoT (SPEC SS3.1.3)
- Certificate renewal: handled automatically by autocert
- Fallback: if ACME fails, serve with expired cert and log error

**Acceptance Criteria:**
- [ ] Loads TLS certificate from file paths
- [ ] Returns error for invalid/missing cert files
- [ ] ACME manager initializes with domain and email from config
- [ ] tls.Config supports TLS 1.2 minimum, prefers TLS 1.3
- [ ] Same TLS config usable by both DoH and DoT servers
- [ ] `go test ./internal/tls/...` passes

**Dependencies:** Task 3
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS3.1.2, SS3.1.3; IMPLEMENTATION.md SS3.2 (tls module)

---

### Task 16: DNS-over-HTTPS (DoH) Server

**Implement DoH endpoint per RFC 8484.**

**Files to create:**
- `internal/doh/server.go` -- DoH HTTP server with Start()/Stop()
- `internal/doh/handler.go` -- HTTP handler: accept POST (application/dns-message) and GET (?dns= base64url), parse to domain.Query, call pipeline, return DNS wire format

**Code requirements:**
- HTTPS server using TLS config from TLSManager (Task 15)
- POST handler: read body as DNS wire format, maximum 512 bytes
- GET handler: decode ?dns= parameter from base64url to DNS wire format
- Response: application/dns-message content type with DNS wire format body
- Status codes: 200 for success, 400 for bad request, 503 for overloaded
- Uses the same pipeline as standard DNS server

**Acceptance Criteria:**
- [ ] DoH POST with valid DNS wire format returns valid DNS response
- [ ] DoH GET with base64url ?dns= parameter works
- [ ] Invalid request body returns 400
- [ ] TLS handshake succeeds with valid certificate
- [ ] Blocked domain via DoH returns 0.0.0.0
- [ ] `curl -H "Content-Type: application/dns-message" --data-binary @query.bin https://localhost/dns-query` works
- [ ] `go test ./internal/doh/...` passes

**Dependencies:** Task 9, Task 15
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.1.2; IMPLEMENTATION.md SS3.2 (doh module)

---

### Task 17: DNS-over-TLS (DoT) Server

**Implement DoT listener per RFC 7858.**

**Files to create:**
- `internal/dot/server.go` -- DoT TLS listener with Start()/Stop()
- `internal/dot/handler.go` -- TLS connection handler: read DNS wire format from TLS stream, call pipeline, return response

**Code requirements:**
- TLS listener on configurable port (default 853) using TLSManager
- DNS wire format inside TLS stream (2-byte length prefix + DNS message per RFC 7858)
- Support TCP keepalive for persistent connections
- Uses the same pipeline as standard DNS and DoH
- Reject non-TLS connections on DoT port

**Acceptance Criteria:**
- [ ] DoT connection with valid TLS succeeds
- [ ] DNS query over TLS returns correct response
- [ ] Blocked domain via DoT returns 0.0.0.0
- [ ] Persistent connection: multiple queries on same TLS connection work
- [ ] Plain DNS to DoT port is rejected
- [ ] `kdig +tls @localhost -p 853 example.com` works
- [ ] `go test ./internal/dot/...` passes

**Dependencies:** Task 9, Task 15
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS3.1.3; IMPLEMENTATION.md SS3.2 (dot module)

---

## Phase 5: DHCP Server

> Implements DHCPv4 server with lease management.
> After this phase: Mantis serves as DHCP + DNS server.

### Task 18: DHCPv4 Server

**Implement DHCPv4 server with IP pool management.**

**Files to create:**
- `internal/dhcp/server.go` -- DHCP server using insomniacslk/dhcp, listens on configurable interface
- `internal/dhcp/handler.go` -- Handles DISCOVER, OFFER, REQUEST, ACK, NAK, RELEASE, INFORM messages
- `internal/dhcp/pool.go` -- IPPool: manages available IPs in configured range, allocate, release, track used IPs
- `internal/dhcp/server_test.go` -- Tests for pool allocation, lease flow

**Code requirements:**
- Use github.com/insomniacslk/dhcp for DHCPv4 protocol
- IP pool from configured range (rangeStart to rangeEnd)
- Sets Mantis IP as DNS server in DHCP options (SPEC SS3.3.1)
- Sets gateway and subnet mask from config
- Pool exhaustion: return NAK, log critical warning

**Acceptance Criteria:**
- [ ] DHCP server listens on configured interface
- [ ] DISCOVER -> OFFER -> REQUEST -> ACK flow works
- [ ] Assigned IP is within configured range
- [ ] DNS server option points to Mantis
- [ ] Gateway and subnet mask set correctly
- [ ] Pool exhaustion returns NAK
- [ ] `go test ./internal/dhcp/...` passes

**Dependencies:** Task 2, Task 3
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS3.3.1; IMPLEMENTATION.md SS3.2 (dhcp module)

---

### Task 19: DHCP Lease Persistence and Static Leases

**Add lease persistence to Pebble and static lease support.**

**Files to modify:**
- `internal/dhcp/handler.go` -- Persist leases on ACK, load on startup, handle static reservations
- `internal/dhcp/pool.go` -- Reserve IPs for static leases, exclude from dynamic pool

**Files to create:**
- `internal/dhcp/lease.go` -- Lease lifecycle: creation, renewal, expiry check, static vs dynamic

**Code requirements:**
- All leases persisted to Pebble via LeaseRepository (Task 4)
- On startup: load all active leases from Pebble, mark IPs as used in pool
- Static leases: MAC -> IP mapping that never expires, excluded from dynamic pool
- Lease renewal: extend expiry on REQUEST for existing lease
- Lease expiry: background goroutine checks every 60s, removes expired leases
- Duplicate MAC handling: update hostname, keep IP reservation

**Acceptance Criteria:**
- [ ] Leases survive application restart (persisted to Pebble)
- [ ] Static lease: MAC always gets same IP regardless of pool
- [ ] Lease renewal extends expiry time
- [ ] Expired leases cleaned up and IPs returned to pool
- [ ] Static lease IP excluded from dynamic pool
- [ ] `go test ./internal/dhcp/...` passes

**Dependencies:** Task 4, Task 18
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.3.1; IMPLEMENTATION.md SS3.2 (dhcp module)

---

## Phase 6: REST API

> Builds the admin REST API with authentication, all endpoints, and WebSocket.
> After this phase: Mantis is fully controllable via API.

### Task 20: API Router and Middleware

**Set up chi router with security middleware and standard response helpers.**

**Files to create:**
- `internal/api/router.go` -- chi.Router setup with all routes from IMPLEMENTATION.md SS5.1
- `internal/api/middleware.go` -- Auth middleware (session + API key), rate limiter (token bucket), CORS, security headers, request ID, logging
- `internal/api/response.go` -- JSON response helpers: Success(), Error(), Paginated(). Standard format from IMPLEMENTATION.md SS5.2

**Code requirements:**
- chi v5 router with route grouping (IMPLEMENTATION.md SS2.7)
- Auth middleware: check HTTP-only cookie for session, or X-Api-Key header for API key
- Rate limiter: golang.org/x/time/rate, per-session token bucket, 60 req/min default
- CORS: allow same origin only (SPA is embedded)
- Security headers from IMPLEMENTATION.md SS10.3
- Request ID: X-Request-Id header with UUID
- Logging: zerolog middleware logging method, path, status, latency

**Acceptance Criteria:**
- [ ] Router registers all 32 routes from IMPLEMENTATION.md SS5.1
- [ ] Unauthenticated request to protected endpoint returns 401
- [ ] Valid session cookie allows access
- [ ] Valid API key in header allows access
- [ ] Rate limit: 61st request within 1 minute returns 429
- [ ] X-RateLimit-* headers present in response
- [ ] Security headers (CSP, X-Frame-Options, etc.) set on all responses
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 2, Task 3
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS6; IMPLEMENTATION.md SS2.7, SS5, SS10

---

### Task 21: Authentication Endpoints

**Implement login, logout, session management, and API key generation.**

**Files to create:**
- `internal/api/auth.go` -- Login(), Logout(), CreateAPIKey(), ListAPIKeys(), DeleteAPIKey() handlers
- `internal/api/setup.go` -- First-run setup handler: create admin account if none exists

**Code requirements:**
- Login: validate username/password against bcrypt hash in Pebble, create session (256-bit random token), set HTTP-only Secure SameSite=Strict cookie
- Logout: delete session from Pebble, clear cookie
- First-run setup: if no admin account exists, present setup endpoint (POST /api/v1/auth/setup)
- API key: generate 64-char random token, store hashed (SHA-256), return plaintext once
- Session duration: 24h sliding window (extend on each valid request)
- Login rate limit: 5 attempts/min per IP

**Acceptance Criteria:**
- [ ] First-run setup creates admin account with hashed password
- [ ] Login with correct credentials returns session cookie
- [ ] Login with wrong password returns 401
- [ ] Login rate limited to 5 attempts/min per IP
- [ ] Authenticated request extends session expiry
- [ ] Logout invalidates session
- [ ] API key generated and returned plaintext once
- [ ] API key authentication works via X-Api-Key header
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 4, Task 20
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS6.3, SS8; IMPLEMENTATION.md SS5.4

---

### Task 22: Statistics Aggregation Engine

**Implement time-bucketed statistics computed from query log events.**

**Files to create:**
- `internal/stats/collector.go` -- StatsCollector: subscribes to event bus, updates in-memory counters
- `internal/stats/aggregator.go` -- Aggregator: hourly/daily buckets, top domains, top clients. Flush to Pebble.
- `internal/stats/aggregator_test.go` -- Tests for bucket rollover, top-N calculation

**Code requirements:**
- Subscribe to event bus (Task 13), process QueryEvent stream
- Maintain current-hour bucket in memory (fast updates)
- Flush to Pebble on hour rollover (stat:hour:timestamp key)
- Daily buckets computed from hourly aggregates
- Track: total queries, blocked count, cache hits, per-domain counts, per-client counts
- Top-N: maintain min-heap for top 100 domains and clients

**Acceptance Criteria:**
- [ ] Total query count accurate after processing 10,000 events
- [ ] Blocked percentage calculated correctly
- [ ] Top 10 domains/clients returned in correct order
- [ ] Hourly bucket flushed to Pebble at hour boundary
- [ ] Dashboard summary computable from last 24 hourly buckets
- [ ] `go test ./internal/stats/...` passes

**Dependencies:** Task 4, Task 13
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.4.2; IMPLEMENTATION.md SS3.2 (stats module)

---

### Task 23: Stats and Query Log API Endpoints

**Implement API endpoints for dashboard statistics and query log.**

**Files to create:**
- `internal/api/stats.go` -- Summary(), OverTime(), TopDomains(), TopClients() handlers
- `internal/api/queries.go` -- List() handler with pagination, filtering by domain/client/result/time range

**Code requirements:**
- GET /stats/summary: total queries (24h/7d/30d), blocked %, cache hit ratio, active clients
- GET /stats/overtime: time-series data, hourly resolution for 24h, daily for 30d
- GET /stats/top-domains: top 10 queried domains (allowed and blocked separately)
- GET /stats/top-clients: top 10 clients by query volume
- GET /queries: paginated query log with filters (domain, client IP, result type, time range)
- Offset-based pagination: ?page=1&perPage=50

**Acceptance Criteria:**
- [ ] /stats/summary returns correct totals for last 24h
- [ ] /stats/overtime returns 24 hourly data points for last 24h
- [ ] /stats/top-domains returns 10 items in descending order
- [ ] /queries returns paginated results with total count
- [ ] /queries?domain=example.com filters correctly
- [ ] /queries?result=blocked filters blocked-only queries
- [ ] All endpoints require authentication
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 20, Task 21, Task 22
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.4, SS6.2

---

### Task 24: Blocklist and Rules API Endpoints

**Implement CRUD API endpoints for blocklist sources and custom rules.**

**Files to create:**
- `internal/api/blocklists.go` -- List(), Create(), Update(), Delete() handlers for BlocklistSource
- `internal/api/gravity.go` -- Rebuild() (trigger manual rebuild), Status() (current gravity stats)
- `internal/api/rules.go` -- List(), Create(), Delete() handlers for CustomRule

**Code requirements:**
- Blocklist CRUD: validate URL, persist to Pebble, trigger Gravity rebuild on add/remove if auto-rebuild enabled
- Gravity rebuild: POST /gravity/rebuild triggers async rebuild, returns 202 Accepted
- Gravity status: returns domain count, last rebuild time, per-source stats
- Rules CRUD: validate domain/pattern, add to Gravity immediately (Task 12)
- Support wildcard and regex pattern validation

**Acceptance Criteria:**
- [ ] POST /blocklists creates new source in Pebble
- [ ] DELETE /blocklists/:id removes source
- [ ] POST /gravity/rebuild triggers rebuild and returns 202
- [ ] GET /gravity/status returns domain count and last rebuild time
- [ ] POST /rules with valid domain adds rule immediately
- [ ] POST /rules with invalid regex returns 400
- [ ] DELETE /rules/:id removes rule and updates Gravity
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 11, Task 12, Task 20, Task 21
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.2, SS6.2

---

### Task 25: DHCP, Clients, and Settings API Endpoints

**Implement remaining API endpoints for DHCP, client management, and settings.**

**Files to create:**
- `internal/api/dhcp.go` -- ListLeases(), CreateStaticLease(), DeleteStaticLease(), GetConfig(), UpdateConfig()
- `internal/api/clients.go` -- ListClients(), SetBlocking() (per-client blocking toggle)
- `internal/api/settings.go` -- GetSettings(), UpdateSettings()

**Code requirements:**
- DHCP leases: read from LeaseRepository, show active + static leases
- Static lease: validate MAC format, IP range, persist via DHCP server
- Client list: aggregate from query log (unique client IPs with hostnames from DHCP)
- Per-client blocking: stored in settings, checked by ClientRuleHandler in pipeline (Task 9)
- Settings: get/set all config values, validate before applying, persist to Pebble

**Acceptance Criteria:**
- [ ] GET /dhcp/leases returns active and static leases
- [ ] POST /dhcp/leases/static creates reservation
- [ ] PUT /clients/:ip/blocking toggles blocking for specific client
- [ ] GET /settings returns all current settings
- [ ] PUT /settings validates and persists changes
- [ ] Settings changes take effect without restart
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 19, Task 20, Task 21
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS3.3, SS3.5, SS3.6, SS6.2

---

### Task 26: WebSocket Live Query Stream

**Implement real-time query log streaming via WebSocket.**

**Files to modify:**
- `internal/api/queries.go` -- Add Stream() handler for WebSocket connection

**Code requirements:**
- GET /queries/stream upgrades to WebSocket (gorilla/websocket)
- Subscribe to event bus, forward QueryEvent as JSON to connected clients
- Support multiple concurrent WebSocket connections (SPEC: 100 max)
- Connection lifecycle: authenticate -> subscribe -> stream -> cleanup on disconnect
- Heartbeat: ping every 30s to detect stale connections
- Backpressure: if client is slow, buffer up to 1000 events, then drop oldest

**Acceptance Criteria:**
- [ ] WebSocket connection established with valid session
- [ ] New DNS queries appear as JSON messages in real-time
- [ ] Multiple clients receive same events
- [ ] Disconnected client cleaned up (subscription removed)
- [ ] Unauthenticated WebSocket attempt rejected
- [ ] 100 concurrent connections handled without performance degradation
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 13, Task 20, Task 21
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS3.4.1, SS6.2

---

### Task 27: System Info and Health Endpoints

**Implement system health check, version info, DNS test, and restart endpoints.**

**Files to create:**
- `internal/api/system.go` -- Info(), Health(), RestartDNS() handlers
- `internal/api/dnstest.go` -- Test() handler: resolve a domain and return result for debugging

**Code requirements:**
- GET /system/health: check DNS server running, Pebble accessible, Gravity loaded. Return component status (IMPLEMENTATION.md SS11.3)
- GET /system/info: version, uptime, Go version, goroutine count, memory stats, Pebble stats
- POST /system/restart-dns: gracefully restart DNS server (drain + rebind)
- GET /dns/test?domain=example.com: resolve domain and return detailed result (latency, upstream used, cached, blocked status)

**Acceptance Criteria:**
- [ ] /system/health returns 200 with component statuses when healthy
- [ ] /system/health returns 503 if critical component is down
- [ ] /system/info returns version, uptime, and resource usage
- [ ] /system/restart-dns restarts DNS without dropping active queries
- [ ] /dns/test?domain=google.com returns resolved IP and latency
- [ ] /dns/test?domain=[blocked] returns blocked status
- [ ] `go test ./internal/api/...` passes

**Dependencies:** Task 20, Task 21
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS6.2; IMPLEMENTATION.md SS11.3

---

## Phase 7: Frontend Foundation

> Sets up the React SPA with routing, state management, and shared components.
> After this phase: SPA loads, navigates between empty pages, authenticates.

### Task 28: React SPA Scaffolding

**Create the React/TypeScript project with Vite, Tailwind, and routing.**

**Files to create:**
- `web/package.json` -- Dependencies: react, react-dom, react-router-dom, @tanstack/react-query, zustand, recharts, tailwindcss, typescript
- `web/vite.config.ts` -- Vite config with React plugin, API proxy to Go backend in dev
- `web/tailwind.config.ts` -- Tailwind config with dark mode (class strategy), custom colors
- `web/tsconfig.json` -- Strict TypeScript
- `web/.eslintrc.cjs` -- ESLint config with React + TypeScript rules
- `web/index.html` -- Root HTML with div#root
- `web/src/main.tsx` -- React entry: QueryClient provider, RouterProvider, Zustand stores
- `web/src/App.tsx` -- Router setup with all routes from IMPLEMENTATION.md SS6.3

**Commands to run:**
```bash
cd web && npm create vite@latest . -- --template react-ts
npm install react-router-dom @tanstack/react-query zustand recharts
npm install -D tailwindcss @tailwindcss/vite
```

**Acceptance Criteria:**
- [ ] `cd web && npm run dev` starts dev server
- [ ] `cd web && npm run build` produces production bundle in web/dist/
- [ ] `cd web && npm run lint` passes
- [ ] All routes from IMPLEMENTATION.md SS6.3 defined (placeholder pages)
- [ ] Tailwind classes work (test with a colored div)
- [ ] Dark mode toggle works via CSS class

**Dependencies:** None (frontend independent of backend at this stage)
**Effort:** 2 hours
**Refs:** IMPLEMENTATION.md SS6

---

### Task 29: Layout, Sidebar, and Shared Components

**Build the app layout shell with navigation sidebar and reusable UI components.**

**Files to create:**
- `web/src/components/Layout.tsx` -- Sidebar + content area layout
- `web/src/components/Sidebar.tsx` -- Navigation links for all 8 pages, active indicator, collapse toggle
- `web/src/components/DataTable.tsx` -- Generic sortable, filterable table with pagination
- `web/src/components/StatCard.tsx` -- Dashboard summary card (icon, label, value, trend)
- `web/src/components/SearchInput.tsx` -- Debounced search input
- `web/src/components/Toggle.tsx` -- On/off toggle switch
- `web/src/components/Modal.tsx` -- Modal dialog with backdrop
- `web/src/components/Toast.tsx` -- Notification toast (success, error, info)
- `web/src/components/ThemeToggle.tsx` -- Dark/light mode switcher
- `web/src/store/uiStore.ts` -- Theme preference, sidebar collapsed state

**Code requirements:**
- Desktop-first layout: fixed sidebar (240px) + scrollable content area
- Sidebar collapses to icons at 1024px breakpoint
- Mobile (<768px): sidebar hidden, hamburger menu
- Dark mode as default (SPEC SS7.3)
- All components use Tailwind utility classes
- DataTable: column sort, pagination, loading skeleton

**Acceptance Criteria:**
- [ ] Layout renders with sidebar and content area
- [ ] Navigation links highlight active route
- [ ] Sidebar collapses at 1024px breakpoint
- [ ] Dark/light theme toggle works and persists to localStorage
- [ ] DataTable renders with sort and pagination
- [ ] Modal opens/closes with backdrop click and Escape key
- [ ] Toast shows and auto-dismisses after 5 seconds
- [ ] `npm run lint` passes

**Dependencies:** Task 28
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS7; IMPLEMENTATION.md SS6.1, SS6.4

---

### Task 30: Frontend Authentication

**Implement login page, auth state management, and route guards.**

**Files to create:**
- `web/src/pages/Login.tsx` -- Login form with username/password, error handling, loading state
- `web/src/store/authStore.ts` -- Zustand store: isAuthenticated, login(), logout(), checkSession()
- `web/src/api/client.ts` -- Fetch wrapper: adds credentials, handles 401 redirect to login
- `web/src/hooks/useAuth.ts` -- Hook wrapping authStore for components

**Code requirements:**
- Login page: centered card, username + password fields, submit button, error message
- Auth store: call POST /auth/login, store auth state, call POST /auth/logout
- Route guard: redirect to /login if not authenticated
- API client: fetch wrapper with `credentials: 'include'` for cookies
- On 401 response: clear auth state, redirect to login
- First-run detection: if GET /auth/status returns "setup_required", show setup form

**Acceptance Criteria:**
- [ ] Login page renders with form
- [ ] Successful login redirects to dashboard
- [ ] Failed login shows error message
- [ ] Unauthenticated route access redirects to login
- [ ] Logout clears session and redirects to login
- [ ] 401 from API triggers redirect to login
- [ ] `npm run lint` passes

**Dependencies:** Task 28, Task 29
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS8.1; IMPLEMENTATION.md SS5.4, SS6.2

---

### Task 31: API Client Layer (TanStack Query Hooks)

**Create all TanStack Query hooks for API data fetching.**

**Files to create:**
- `web/src/api/queries.ts` -- useQueryLog(), useQueryLogStream() (WebSocket)
- `web/src/api/stats.ts` -- useStatsSummary(), useStatsOverTime(), useTopDomains(), useTopClients()
- `web/src/api/blocklists.ts` -- useBlocklists(), useCreateBlocklist(), useDeleteBlocklist(), useRebuildGravity()
- `web/src/api/rules.ts` -- useRules(), useCreateRule(), useDeleteRule()
- `web/src/api/dhcp.ts` -- useLeases(), useCreateStaticLease(), useDeleteStaticLease(), useDhcpConfig()
- `web/src/api/settings.ts` -- useSettings(), useUpdateSettings()
- `web/src/api/websocket.ts` -- WebSocket client for live query stream
- `web/src/hooks/useWebSocket.ts` -- React hook for WebSocket lifecycle, auto-reconnect
- `web/src/utils/format.ts` -- Number formatting (1234 -> 1.2K), date formatting, domain truncation
- `web/src/utils/constants.ts` -- API base URL, default page size, refresh intervals

**Code requirements:**
- TanStack Query v5 with QueryClient configured (staleTime, refetchInterval)
- Stats: auto-refetch every 10 seconds
- Query log: manual fetch + WebSocket live stream
- Mutations: optimistic updates for toggles (blocking, enable/disable blocklist)
- WebSocket: auto-reconnect with exponential backoff

**Acceptance Criteria:**
- [ ] All API endpoints have corresponding TanStack Query hooks
- [ ] Stats queries auto-refetch every 10s
- [ ] Mutations invalidate related queries on success
- [ ] WebSocket hook connects, receives events, auto-reconnects on disconnect
- [ ] Format utilities handle edge cases (0, undefined, very large numbers)
- [ ] TypeScript types match API response shapes
- [ ] `npm run lint` passes

**Dependencies:** Task 28, Task 30
**Effort:** 4 hours
**Refs:** IMPLEMENTATION.md SS6.2

---

## Phase 8: Frontend Features

> Builds all 8 page components with real data from the API.
> After this phase: complete admin UI.

### Task 32: Dashboard Page

**Implement the main dashboard with summary cards, charts, and top lists.**

**Files to create:**
- `web/src/pages/Dashboard.tsx` -- Dashboard page composition
- `web/src/components/Chart.tsx` -- Recharts wrapper for line/bar charts

**Code requirements:**
- 4 summary cards: total queries (24h), blocked percentage, cache hit rate, active clients
- 24h query chart: line chart with hourly resolution, blocked/allowed stacked
- Top 10 blocked domains: table with domain name and block count
- Top 10 allowed domains: table with domain name and query count
- Quick action: toggle blocking on/off (global disable)
- Loading skeletons while data fetches

**Acceptance Criteria:**
- [ ] Summary cards show correct values from /stats/summary
- [ ] Query chart renders with correct 24h data
- [ ] Top domains tables populated and sorted
- [ ] Loading skeletons show during API fetch
- [ ] Data refreshes every 10 seconds
- [ ] Responsive: cards stack vertically on mobile
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS7.2 (Dashboard)

---

### Task 33: Query Log Page

**Implement real-time query log with search, filters, and WebSocket streaming.**

**Files to create/modify:**
- `web/src/pages/QueryLog.tsx` -- Full query log page

**Code requirements:**
- DataTable with columns: timestamp, domain, client, type, result, latency
- Search bar: filter by domain (debounced 300ms)
- Filter buttons: All, Allowed, Blocked, Cached
- Client filter: dropdown of known clients
- Live stream toggle: enables WebSocket, new rows prepend at top with animation
- Inline actions: click domain to add to allow/blocklist
- Export button: download filtered results as CSV
- Pagination: 50 rows per page, offset-based

**Acceptance Criteria:**
- [ ] Table displays query log entries from API
- [ ] Search by domain filters results in real-time
- [ ] Filter by result type (blocked/allowed) works
- [ ] Live toggle streams new queries via WebSocket
- [ ] New entries animate in at top of table
- [ ] Click domain shows allow/block action menu
- [ ] Pagination works correctly
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 4 hours
**Refs:** SPECIFICATION.md SS7.2 (Query Log)

---

### Task 34: Blocklist Management Page

**Implement blocklist source management with stats and Gravity rebuild trigger.**

**Files to create/modify:**
- `web/src/pages/Blocklists.tsx` -- Blocklist management page

**Code requirements:**
- Table: blocklist sources with name, URL, domain count, last update, status, enabled toggle
- Add blocklist modal: name, URL, format selector (hosts/domains/adblock)
- Enable/disable toggle: optimistic update, toggle enabled field
- Delete: confirmation modal
- Gravity rebuild button: triggers rebuild, shows progress/status
- Per-list stats: domain count contribution
- Default blocklists highlighted on first setup

**Acceptance Criteria:**
- [ ] Lists all blocklist sources with stats
- [ ] Add new blocklist source via modal
- [ ] Toggle enable/disable with optimistic update
- [ ] Delete with confirmation dialog
- [ ] Gravity rebuild button triggers rebuild and shows status
- [ ] Domain counts per list displayed
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS7.2 (Blocklists)

---

### Task 35: Custom Rules Page

**Implement custom allow/block rule management.**

**Files to create/modify:**
- `web/src/pages/Rules.tsx` -- Custom rules page

**Code requirements:**
- Two tabs: Block rules and Allow rules
- Table: domain, type (exact/wildcard/regex), comment, created date, delete action
- Add rule form: domain input, type selector, optional comment
- Wildcard validation: show warning for overly broad patterns
- Import/export: paste text area for bulk import, download as text file
- Search: filter rules by domain

**Acceptance Criteria:**
- [ ] Tabs switch between block and allow rules
- [ ] Add rule with domain, type, and comment
- [ ] Rule takes effect immediately (verify via /dns/test endpoint)
- [ ] Delete rule with confirmation
- [ ] Bulk import from text (one domain per line)
- [ ] Export rules as text file
- [ ] Search filters rules by domain
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS7.2 (Custom Rules)

---

### Task 36: DHCP Management Page

**Implement DHCP lease viewer, static lease management, and pool configuration.**

**Files to create/modify:**
- `web/src/pages/Dhcp.tsx` -- DHCP management page

**Code requirements:**
- Active leases table: MAC, IP, hostname, lease end, is static
- Static leases section: table + add form (MAC, IP, hostname)
- Pool configuration form: range start, range end, gateway, subnet mask, lease duration
- Lease expiry: show remaining time, highlight expiring soon
- Delete static lease with confirmation

**Acceptance Criteria:**
- [ ] Active leases displayed with all fields
- [ ] Add static lease with MAC and IP
- [ ] Delete static lease with confirmation
- [ ] Pool configuration form loads current values
- [ ] Pool config save validates IP ranges
- [ ] Lease time shown as human-readable duration
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS7.2 (DHCP)

---

### Task 37: Network (Clients) Page

**Implement network client overview with per-client blocking.**

**Files to create/modify:**
- `web/src/pages/Network.tsx` -- Network clients page

**Code requirements:**
- Client table: hostname, IP, MAC (from DHCP), query count (24h), blocking toggle
- Sort by query count (most active first)
- Per-client blocking toggle: optimistic update
- Click client: expand to show recent query history (last 50 queries)
- Rename client: editable hostname field
- Identify unknown clients: show "Unknown" for clients without DHCP hostname

**Acceptance Criteria:**
- [ ] Client list shows all known devices
- [ ] Query count per client displayed
- [ ] Blocking toggle works per client
- [ ] Expanded view shows recent queries for client
- [ ] Sort by different columns works
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS7.2 (Network)

---

### Task 38: Settings Page

**Implement system settings with tabbed form.**

**Files to create/modify:**
- `web/src/pages/Settings.tsx` -- Settings page with tabs

**Code requirements:**
- Tabs: DNS, DHCP, Encryption, Logging, System
- DNS tab: upstream servers (add/remove), resolver mode toggle (forward/recursive), cache size, blocking mode
- DHCP tab: enable/disable, interface, range, gateway, subnet, lease duration
- Encryption tab: DoH/DoT enable/disable, cert file paths, ACME domain/email, ACME enable
- Logging tab: query log level, retention days, privacy mode toggle
- System tab: API key management (generate/revoke), password change, data export
- Save button per tab, validation errors inline

**Acceptance Criteria:**
- [ ] All settings from config displayed in correct tabs
- [ ] Changes persist on save
- [ ] Validation errors shown inline
- [ ] API key generation works (show once, then masked)
- [ ] Password change requires current password
- [ ] Settings take effect without restart
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS7.2 (Settings)

---

### Task 39: Long-term Statistics Page

**Implement historical statistics with date range selection.**

**Files to create/modify:**
- `web/src/pages/Statistics.tsx` -- Long-term stats page

**Code requirements:**
- Date range picker: presets (7d, 30d, 90d) + custom range
- Charts: queries per day (line), block rate over time (line), top domains over time
- Summary row: total queries, total blocked, avg block rate for selected range
- Queries per day: stacked bar (allowed/blocked)
- Export: download chart data as CSV

**Acceptance Criteria:**
- [ ] Date range picker with presets and custom range
- [ ] Queries per day chart renders with correct data
- [ ] Block rate trend chart works
- [ ] Summary numbers update on range change
- [ ] Export downloads CSV
- [ ] `npm run lint` passes

**Dependencies:** Task 29, Task 31
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS7.2 (Long-term Statistics)

---

## Phase 9: Integration and Polish

> Embeds frontend in Go binary, adds theming polish, responsive design, and tests.
> After this phase: single binary with embedded UI works end-to-end.

### Task 40: Embed React SPA in Go Binary

**Configure Go embed to serve the React SPA from the compiled binary.**

**Files to create:**
- `internal/web/embed.go` -- `//go:embed` directive for web/dist/, http.FileServer serving SPA
- `internal/web/spa.go` -- SPA fallback handler: serve index.html for all non-API, non-asset routes

**Files to modify:**
- `internal/api/router.go` -- Mount SPA handler at root, API routes at /api/v1/
- `Makefile` -- Add `build-frontend` target, `build-all` target that builds frontend then Go

**Code requirements:**
- `//go:embed all:../../web/dist` embeds entire built SPA
- SPA fallback: any route not matching /api/* or a static file serves index.html
- Static assets served with cache headers (hashed filenames from Vite)
- Gzip compression for text assets

**Acceptance Criteria:**
- [ ] `make build-all` builds frontend then Go binary
- [ ] Single binary serves React SPA at http://localhost:8080/
- [ ] Client-side routing works (refresh on /queries loads correctly)
- [ ] API calls from SPA to /api/v1/* work
- [ ] Static assets served with cache headers
- [ ] Binary size < 30MB

**Dependencies:** Task 28-39 (all frontend tasks), Task 20
**Effort:** 2 hours
**Refs:** IMPLEMENTATION.md SS1.2, SS11

---

### Task 41: Dark/Light Theme and Visual Polish

**Ensure consistent dark/light theming across all pages.**

**Files to modify:**
- All page and component files in `web/src/`

**Code requirements:**
- Dark mode as default (SPEC SS7.3)
- Theme toggle persists to localStorage
- All components respect dark mode (backgrounds, text, borders, charts)
- Chart colors adapt to theme
- Form inputs styled consistently in both themes
- Focus states visible in both themes (accessibility)

**Acceptance Criteria:**
- [ ] Dark mode is default on first visit
- [ ] Light mode toggle works and persists
- [ ] No hard-to-read text in either theme
- [ ] Charts readable in both themes
- [ ] Form focus states visible in both themes
- [ ] `npm run lint` passes

**Dependencies:** Task 40
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS7.3

---

### Task 42: Responsive Design

**Ensure the UI works across desktop, tablet, and mobile screen sizes.**

**Files to modify:**
- Layout, Sidebar, and all page components

**Code requirements:**
- Desktop (>1024px): full sidebar, multi-column layouts
- Tablet (768-1024px): collapsed sidebar (icons only), adjusted grids
- Mobile (<768px): hidden sidebar with hamburger menu, stacked layouts, simplified tables
- DataTable: horizontal scroll on mobile
- Charts: responsive container, simplified on mobile
- Touch targets: minimum 44x44px on mobile

**Acceptance Criteria:**
- [ ] Layout works at 1440px, 1024px, 768px, and 375px widths
- [ ] Sidebar collapses to icons at 1024px
- [ ] Sidebar hidden with hamburger at 768px
- [ ] Tables horizontally scrollable on mobile
- [ ] No horizontal overflow on any screen size
- [ ] `npm run lint` passes

**Dependencies:** Task 41
**Effort:** 3 hours
**Refs:** SPECIFICATION.md SS7.3

---

### Task 43: End-to-End Tests

**Write E2E tests for critical user flows.**

**Files to create:**
- `tests/e2e/setup_test.go` -- Test helper: start Mantis, wait for ready, cleanup
- `tests/e2e/dns_test.go` -- DNS resolution tests: blocked domain, allowed domain, cache hit
- `tests/e2e/api_test.go` -- API tests: login, add blocklist, add rule, query log
- `tests/e2e/dhcp_test.go` -- DHCP tests: discover/offer/request/ack flow

**Code requirements:**
- Start real Mantis instance in test (random ports)
- DNS tests: send actual DNS queries with miekg/dns client
- API tests: HTTP requests to API endpoints
- DHCP tests: simulate DHCP client flow
- Cleanup: stop server, remove temp data directory

**Acceptance Criteria:**
- [ ] Login + dashboard data loads
- [ ] Add blocklist source + trigger Gravity rebuild + domain blocked in DNS
- [ ] Add allow rule + previously blocked domain now allowed
- [ ] Query log shows the test queries
- [ ] DHCP lease assignment works end-to-end
- [ ] `go test ./tests/e2e/...` passes (may need sudo for port 53/67)

**Dependencies:** All backend tasks (1-27)
**Effort:** 4 hours
**Refs:** IMPLEMENTATION.md SS9

---

## Phase 10: Release Preparation

> Containerization, CI/CD, documentation, and release artifacts.
> After this phase: Mantis is shippable.

### Task 44: Dockerfile and Docker Compose

**Create production Docker build and development compose file.**

**Files to create:**
- `docker/Dockerfile` -- Multi-stage build from IMPLEMENTATION.md SS11.2
- `docker/docker-compose.yml` -- Example compose with volume mounts, port mapping, environment vars

**Code requirements:**
- 3-stage Dockerfile: Node build -> Go build -> scratch runtime (IMPLEMENTATION.md SS11.2)
- Multi-arch: buildx for linux/amd64 and linux/arm64
- HEALTHCHECK instruction using /api/v1/system/health
- Volume: /data for Pebble database and config
- Expose: 53/udp, 53/tcp, 67/udp, 443, 853, 8080
- Docker Compose: single service with example config

**Acceptance Criteria:**
- [ ] `docker build -f docker/Dockerfile -t mantis .` succeeds
- [ ] Docker image < 50MB (scratch base)
- [ ] `docker-compose up` starts Mantis with default config
- [ ] DNS queries work from host to container
- [ ] Admin UI accessible at mapped port
- [ ] HEALTHCHECK passes
- [ ] Data persists across container restarts (volume mount)

**Dependencies:** Task 40
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS9.2; IMPLEMENTATION.md SS11.2

---

### Task 45: Install Script and Systemd Service

**Create one-line installer for Linux with systemd integration.**

**Files to create:**
- `scripts/install.sh` -- Downloads latest release binary, creates /etc/mantis/ config dir, creates systemd service, enables and starts
- `configs/mantis.service` -- Systemd unit file with proper security hardening

**Code requirements:**
- Install script: detect architecture, download from GitHub releases, verify checksum
- Create mantis user/group for service
- Config directory: /etc/mantis/ with example config
- Data directory: /var/lib/mantis/ owned by mantis user
- Systemd service: After=network.target, DynamicUser=yes (or dedicated user), capability binding for ports <1024
- Uninstall instructions in comments

**Acceptance Criteria:**
- [ ] `curl -fsSL https://get.mantis.dev | bash` installs and starts Mantis
- [ ] Systemd service starts on boot
- [ ] `systemctl status mantis` shows running
- [ ] Mantis runs as non-root with CAP_NET_BIND_SERVICE
- [ ] Config file at /etc/mantis/config.toml
- [ ] Logs visible via `journalctl -u mantis`

**Dependencies:** Task 44
**Effort:** 2 hours
**Refs:** SPECIFICATION.md SS9.2

---

### Task 46: CI/CD Pipeline (GitHub Actions)

**Set up GitHub Actions for testing, building, and releasing.**

**Files to create:**
- `.github/workflows/ci.yml` -- PR/push: lint, test, build
- `.github/workflows/release.yml` -- Tag push: build all platforms, create GitHub release, push Docker image

**Code requirements:**
- CI workflow: golangci-lint -> go test -> npm lint -> npm test -> go build
- Matrix build: linux/amd64, linux/arm64, darwin/amd64, darwin/arm64
- Release workflow: triggered on v* tag push
- Build cross-platform binaries with ldflags version injection
- Upload binaries to GitHub Release
- Build and push multi-arch Docker image to ghcr.io
- Cache: Go modules and npm packages

**Acceptance Criteria:**
- [ ] CI runs on every PR and push to main
- [ ] CI fails if lint, test, or build fails
- [ ] Release creates GitHub Release with all platform binaries
- [ ] Docker image pushed to ghcr.io on release
- [ ] Version injected from git tag
- [ ] CI completes in <10 minutes

**Dependencies:** Task 43, Task 44
**Effort:** 3 hours
**Refs:** IMPLEMENTATION.md SS9.3, SS12

---

### Task 47: README and Documentation

**Write comprehensive README and API documentation.**

**Files to create/modify:**
- `README.md` -- Full README: features, quick start, configuration, Docker, build from source, contributing
- `configs/mantis.example.toml` -- Complete example config with all settings and comments
- `docs/API.md` -- API endpoint reference with curl examples

**Code requirements:**
- README: professional, no emojis, clear sections
- Quick start: Docker one-liner and binary install
- Configuration reference: all settings with descriptions
- API docs: every endpoint with curl example and sample response
- Contributing guide: build from source, run tests, PR process
- Badges: CI status, license, latest release

**Acceptance Criteria:**
- [ ] README covers installation (Docker + binary + source)
- [ ] Quick start gets Mantis running in <5 minutes
- [ ] All configuration options documented in example TOML
- [ ] API docs cover all 32 endpoints
- [ ] Build from source instructions work on clean machine
- [ ] No emojis in README

**Dependencies:** Task 44, Task 45
**Effort:** 3 hours
**Refs:** All spec and implementation docs

---

### Task 48: Build Scripts and Release Preparation

**Create cross-compilation scripts and final release checklist.**

**Files to create:**
- `scripts/build.sh` -- Cross-compile for all platforms, create checksums, package releases
- `Makefile` -- Complete with all targets: dev, build, test, lint, clean, build-frontend, build-all, release

**Files to modify:**
- `cmd/mantis/main.go` -- Final wiring: all components initialized, graceful shutdown, version flag

**Code requirements:**
- Build script: compile for linux/amd64, linux/arm64, darwin/amd64, darwin/arm64
- Strip debug symbols (-ldflags="-s -w") for smaller binaries
- Create sha256sum checksums file
- Tar.gz packaging for each platform
- Makefile: dev (run with live reload), build (current platform), build-all (cross-compile)
- Final main.go wiring: config -> storage -> gravity -> pipeline -> DNS -> DoH -> DoT -> DHCP -> API -> serve

**Acceptance Criteria:**
- [ ] `make build-all` produces 4 platform binaries
- [ ] Each binary < 25MB (stripped)
- [ ] Checksums file generated
- [ ] `make dev` starts Mantis in development mode
- [ ] `make test` runs all Go and frontend tests
- [ ] `make lint` runs all linters
- [ ] Main.go initializes all components in correct order
- [ ] Graceful shutdown: SIGINT/SIGTERM drains connections and closes Pebble cleanly

**Dependencies:** All previous tasks
**Effort:** 3 hours
**Refs:** IMPLEMENTATION.md SS11

---

## Milestones

| Milestone           | After Task | What's Achieved                                         | Demo-able?         |
|---------------------|------------|--------------------------------------------------------|--------------------|
| Foundation          | Task 4     | Project compiles, Pebble works, config loads            | Smoke test         |
| DNS Blocking        | Task 9     | Resolves queries, blocks from hardcoded list             | dig queries        |
| Real Blocklists     | Task 13    | Downloads real lists, logs queries                       | Full DNS blocking  |
| All DNS Modes       | Task 17    | Forward + recursive + DoH + DoT                         | Encrypted DNS      |
| DHCP Working        | Task 19    | Full DHCP + DNS server                                  | Network setup      |
| API Complete        | Task 27    | All 32 API endpoints functional                          | curl/API client    |
| Frontend MVP        | Task 39    | All 8 admin pages working                                | Browser demo       |
| Single Binary       | Task 40    | Embedded SPA, one file deployment                        | Binary download    |
| Release Ready       | Task 48    | Docker, CI/CD, docs, cross-platform builds               | Ship it            |

---

## Dependency Graph

```
Phase 1: Foundation
[T1] → [T2] → [T4]
[T1] → [T3]

Phase 2: DNS Core
[T2] → [T5]
[T2] → [T6]
[T2] → [T8]
[T5,T6] → [T7]
[T5,T6,T7,T8] → [T9]

Phase 3: Blocklists
[T8] → [T10]
[T4,T8,T10] → [T11]
[T4,T8] → [T12]
[T4,T9] → [T13]

Phase 4: Advanced DNS
[T6,T8] → [T14]
[T3] → [T15]
[T9,T15] → [T16]
[T9,T15] → [T17]

Phase 5: DHCP
[T2,T3] → [T18]
[T4,T18] → [T19]

Phase 6: API
[T2,T3] → [T20]
[T4,T20] → [T21]
[T4,T13] → [T22]
[T20,T21,T22] → [T23]
[T11,T12,T20,T21] → [T24]
[T19,T20,T21] → [T25]
[T13,T20,T21] → [T26]
[T20,T21] → [T27]

Phase 7: Frontend Foundation
[] → [T28]
[T28] → [T29]
[T28,T29] → [T30]
[T28,T30] → [T31]

Phase 8: Frontend Features
[T29,T31] → [T32-T39]

Phase 9: Integration
[T28-T39,T20] → [T40]
[T40] → [T41]
[T41] → [T42]
[T1-T27] → [T43]

Phase 10: Release
[T40] → [T44]
[T44] → [T45]
[T43,T44] → [T46]
[T44,T45] → [T47]
[ALL] → [T48]
```
